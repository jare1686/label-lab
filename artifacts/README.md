# Artifacts

`artifacts/` holds reproducible outputs from label-lab runs.

Examples:
- cleaned canonical datasets
- cleaning reports
- converted exports such as COCO
- analysis summaries and geometry metrics

Generated outputs should usually live under a run-specific subdirectory such as `artifacts/generated/<run_id>/`.
